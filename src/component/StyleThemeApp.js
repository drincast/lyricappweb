import { makeStyles } from '@material-ui/core/styles';

const StyleThemeApp = makeStyles({
    root: {
        backgroundColor: 'white',
        color: 'black'
    },
    rootBlack:  {
        backgroundColor: 'black',
        color: 'white'
    }
});

export default StyleThemeApp;